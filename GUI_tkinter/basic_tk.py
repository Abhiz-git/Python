import tkinter as tk

root = tk.Tk()          # work as main
root.title("Squares & Roots")

tk.Label(root, text = "come on...\nlets Math..!").pack(pady=21)


root.mainloop()  # mandatory to run the tkinenter always must be a last line